{
  "name": "squash",
  "version": "1.0.0",
  "description": "der drops ist gelutscht",
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --sourcemap --minify --outfile=build/main.js && cp src/main.html build && python3 -m http.server -d build",
    "production": "tsc --noEmit && esbuild src/main.ts --bundle --minify --outfile=build/main.js && cp src/main.html build"
  },
  "author": "Malte",
  "license": "UNLICENSED",
  "devDependencies": {
    "esbuild": "^0.14.25"
  }
}
